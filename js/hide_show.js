/* TITLES SECTION */
const title = ScrollReveal({
    distance: '100px',
    opacity: 0,
    reset: true,
    delay: 50,
    duration: 800
})

title.reveal('#title')
title.reveal('#title', { delay: 400 })

/* SLIDE SHOW SECTION */
const slid_show = ScrollReveal({
    distance: '100px',
    opacity: 0,
    reset: true,
    delay: 50,
    duration: 800
})

slid_show.reveal('#background_skin_awp')
slid_show.reveal('#background_skin_awp', { delay: 400 })

/* TEAM SECTION */
const choose_team = ScrollReveal({
    distance: '100px',
    opacity: 0,
    reset: true,
    delay: 50,
    duration: 800
})

choose_team.reveal('#choose_team')
choose_team.reveal('#choose_team', { delay: 400 })

/* IMG MAP SECTION */
const map_hide_show = ScrollReveal({
    distance: '100px',
    opacity: 0,
    reset: true,
    delay: 50,
    duration: 800
})

map_hide_show.reveal('#map_hide_show')
map_hide_show.reveal('#map_hide_show', { delay: 400 })
